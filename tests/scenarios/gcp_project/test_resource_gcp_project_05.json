{
    "description": "Test illegal configuration: API both enabled and disabled.",
    "actual": {
        "project": {
            "prj": { "projectId": "prj", "name": "prj", "parent": { "type": "organization", "id": "123" } }
        },
        "billing": {
            "prj": { "billingAccountName": "billingAccounts/ABC123" }
        },
        "apis": {
            "prj": [ "api1" ]
        }
    },
    "config": {
        "project_id": "prj",
        "organization_id": 1234,
        "billing_account_id": "ABC123",
        "apis": { "enabled": [ "api1" ], "disabled": [ "api1", "api2", "api3" ] }
    },
    "expected": {
        "exception": "Exception",
        "match": "cannot be both enabled & disabled"
    }
}
